// Array.prototype.map()

// The map() method of Array instances creates a new array populated
// with the results of calling a provided function on every element
// in the calling array.
//
// const array = [1, 4 , 9 , 16 , 23];
//
// const mapped = array.map(x => x * x);
//
// console.log(mapped);
//
// map(callbackfn)
// map(callbackfn , thisArg);

// The map method is an iterative method. it calls a provided callbackfn function once for each element in the array and constructs  a new array
// from resukts

// const num = [1,4,9];
// console.log(num.map(num => Math.sqrt(num)));

// Array.prototype.pop()

// The pop method of Array instances remvoes form the last element from an array and returns element


